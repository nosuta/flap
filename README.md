# godash

Cross-platform bridge core for Flutter + Go/TinyGo applications.

Provides the FFI / Web Worker bridge, SQLite abstraction, RPC/Reverse-RPC/Push transport, and protobuf code generators used by the Flutter + Go hybrid architecture.

This package is currently intended for internal use and is not published to pub.dev.
