name: godash
description: "Cross-platform bridge core for Flutter + Go/TinyGo"
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.12.0
  flutter: ^3.41.0

dependencies:
  ffi: ^2.2.0
  fixnum: ^1.1.1
  flutter:
    sdk: flutter
  logging: ^1.3.0
  path_provider: ^2.1.5
  protobuf: ^6.0.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
